name: pinn-heat
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy=1.24
  - matplotlib=3.7
  - jupyter
  - notebook
  - ipykernel
  - pip
  - pip:
      - torch==2.1.0
      - torchvision==0.16.0
